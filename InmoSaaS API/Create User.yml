info:
  name: Create User
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/users
  body:
    type: json
    data: |-
      {
        "name": "Ana Pérez",
        "email": "ana-sin-arroba-inmosaas.com",
        "password": "password123",
        "phone": "600987654",
        "agencyName": "Costa Inmobiliaria"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
