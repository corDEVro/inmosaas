info:
  name: Create User
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/users
  body:
    type: json
    data: |-
      {
        "name": "Prueba JWT",
        "email": "test@inmosaas.com",
        "password": "miPassword123",
        "phone": "600111222",
        "agencyName": "InmoSaaS Test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
