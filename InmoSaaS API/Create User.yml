info:
  name: Create User
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/users
  body:
    type: json
    data: |-
      {
        "name": "María Asesora",
        "email": "maria@inmosaas.com",
        "password": "password123",
        "phone": "600999888",
        "agencyName": "InmoSaaS Premium"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
